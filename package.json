{
  "name": "jobs-doomsday-clock",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "compute": "node scripts/compute-clock.mjs",
    "refresh-data": "npm run fetch-news && npm run compute",
    "refresh-rich-data": "npm run fetch-news && npm run backfill-media && npm run compute && npm run save-history && npm run backfill-source-groups && npm run validate-rich-signals && npm run validate-rich-history",
    "generate-guidance": "node scripts/generate-occupation-guidance.mjs",
    "enrich-guidance": "node scripts/enrich-guidance.mjs",
    "fetch-news": "node scripts/fetch-news.mjs",
    "validate-rich-signals": "node scripts/validate-rich-signals.mjs",
    "validate-rich-history": "node scripts/validate-rich-signals.mjs public/clock-history.json",
    "save-history": "node scripts/save-history.mjs",
    "backfill-news": "node scripts/backfill-news.mjs",
    "backfill-media": "node scripts/backfill-media-metadata.mjs",
    "backfill-source-groups": "node scripts/backfill-source-groups.mjs"
  },
  "dependencies": {
    "html-to-image": "^1.11.13",
    "mammoth": "^1.12.0",
    "pdf-parse": "^2.4.5",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "@vitejs/plugin-react": "^5.0.4",
    "typescript": "^5.9.3",
    "vite": "^7.1.7"
  }
}
